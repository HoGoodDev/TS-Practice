function greeting(userName: string, age: number) {
    console.log(`Hi ${userName}:D I can't believe ur ${age}!`);
  }
  
  const userName = "BillyBobJoe";
  const age = 19;
  
  greeting(userName, age);
  